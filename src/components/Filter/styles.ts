import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 5,
  },
  title: {
    fontSize: 12,
    fontWeight: 600,
  },
  header: {
    width: "100%",
    flexDirection: "row",
    gap: 12,
  },
});
